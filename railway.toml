[build]
builder = "nixpacks"

[deploy]
# IMPORTANT: seed_rooms and setup_admin removed - run manually only for initial setup
# Running seed_rooms deletes all rooms and recreates dummy data!
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn smartspaceupy.wsgi"
